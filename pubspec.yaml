name: big_fun_app
description: The Big Fun Mobile App

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.2

  #aqui agrego todas las dependencias
  http: ^1.1.0
  sqflite: ^2.3.0
  path: ^1.8.3
  infinite_scroll_pagination: ^4.0.0
  # para el date
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/  # Ruta a la carpeta de assets de imagenes
